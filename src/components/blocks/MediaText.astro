---
import type { MediaTextBlock } from '../../types/block.interface';
import '@wordpress/block-library/build-style/media-text/style.css';

interface Props {
  block: MediaTextBlock;
}
const { block } = Astro.props;
---
<Fragment set:html={block.rendered} />