---
import type Product from '../types/product.interface';

export interface Props {
	product: Product;
}

const { product } = Astro.props;

---

<article>
	<a href={`${new URL(product.permalink).pathname}`}>
		<h2>{product.name}</h2>
	</a>
</article>
<style>
</style>
